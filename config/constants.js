// config/constants.js

// ✅ Constants used across the project

// Name of the Cloudant database where tasks are stored
const TASKS_DB = "tasks"; // Make sure this matches your actual DB name

module.exports = {
  TASKS_DB,
};
